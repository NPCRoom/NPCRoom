package com.npcroom;
import org.springframework.web.bind.annotation.*;
import java.util.*;

// add necessary beans up here (also don't worry if CORS doesn't work, we will deal with that later

public class NPCRoomCrackheadController {
    private String result;

    public void processStatement(@RequestBody String message) {
        // add processing logic (hint: it's pretty similar to the classic controller one. See NPCRoomController.java.
    }
}
